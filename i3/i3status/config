general {
        output_format = "i3bar"
        colors = false
        interval = 10
}

order += "disk /"
order += "memory"
order += "tztime week"
order += "tztime local"

tztime local {
		format = " W:%W \ %a %-d %b %H:%M "
}

disk "/" {
    format = " %avail "
}

memory {
        format = " %used "
        threshold_degraded = "10%"
        format_degraded = "MEMORY: %free"
}
